import React from "react";
import { StyleSheet, Text, View } from "react-native";

const userAreaStyles = StyleSheet.create({
  toucheable: {
    marginBottom: 10,
  },
});

export default userAreaStyles;
