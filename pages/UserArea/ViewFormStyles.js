import { StyleSheet, Text, View } from "react-native";
const ViewFormStyles = StyleSheet.create({
    selectedRadioButon: {
        width: 90,
        height: 30,
        borderRadius: 30,
        borderColor: "#8CC63F",
        borderWidth: 1,
        margin: 5,
    
        justifyContent: "center",
        alignItems: "center",
        // backgroundColor: "#E62321",
      },

});

export default ViewFormStyles;
